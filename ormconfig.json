{
  "type": "sqlite",
  "database": "./database/notes.sqlite",
  "synchronize": false,
  "logging": false,
  "entities": ["src/components/**/*.entity.ts"],
  "migrations": ["src/migration/*.ts"],
  "cli": {
    "migrationsDir": "src/migration"
  }
}
